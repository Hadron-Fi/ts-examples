# Network mode: "litesvm" (default) or "devnet"
NETWORK=devnet

# Required for devnet — path to a Solana keypair JSON file
WALLET=./wallet.json

# RPC endpoint (defaults to public devnet if not set)
RPC_URL=https://api.devnet.solana.com
